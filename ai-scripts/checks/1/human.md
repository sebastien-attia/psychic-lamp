# Human checks — Phase 1 OpenAPI Contract
□ Open the spec in Swagger UI (or editor.swagger.io) — all endpoints render without errors.
□ Response examples look realistic (not just placeholders).
□ The spec captures the training-doc requirements (search, pagination, optimistic locking, audit).
