# BFF master changelog — owns the `bff_session` database
# (SPRING_SESSION, SPRING_SESSION_ATTRIBUTES). Numbering restarts at 001
# under BFF ownership: this is a fresh, BFF-owned migration history,
# distinct from the business-service `boatapp` changelog.
databaseChangeLog:
  - include:
      file: db/changelog/changes/001-create-spring-session-tables.yaml
