# Human checks — Phase 0 Bootstrap
□ CLAUDE.md reads clearly and matches the actual repo layout (both services present, 4 envs documented).
□ .claude/rules/ files each describe a single, focused rule (BFF, business-service, frontend, infra, openapi, testing, git).
□ .env.example is safe to commit — no real secrets.
