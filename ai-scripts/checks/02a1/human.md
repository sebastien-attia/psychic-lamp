# Human checks — Phase 02a1 Backend Scaffold
□ Spot-check the generated API client and server interfaces — method signatures match the OpenAPI spec.
□ The pom.xml for each service declares the openapi-generator-maven-plugin with the right `library`, `apiNameSuffix`, and `globalProperties` filters.
