/**
 * PostCSS configuration: Tailwind v3 engine + autoprefixer.
 * (Vite picks this up automatically when `postcss.config.js` is at the project root.)
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
