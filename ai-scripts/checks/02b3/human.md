# Human checks — Phase 02b3 Frontend Boat List
□ Resize the browser to 375px, 768px, 1024px — layout adapts cleanly, no horizontal scroll.
□ Loading skeleton briefly shows before the data arrives (throttle network to test).
□ Empty state copy is helpful (not just "No data").
